# AWS Redirect LambdaTerraform module

Terraform module which a lambda @edge redirect lambda on AWS.

## Usage

```hcl
module "lambda-redirect" {
  source = "genstackio/website/aws//modules/lambda-redirect"

  // ...
}
```
