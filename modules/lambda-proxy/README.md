# AWS Proxy Lambda Terraform module

Terraform module which a lambda @edge proxy lambda on AWS.

## Usage

```hcl
module "lambda-proxy" {
  source = "genstackio/website/aws//modules/lambda-proxy"

  // ...
}
```
