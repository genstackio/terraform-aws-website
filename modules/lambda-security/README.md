# AWS Security Lambda Terraform module

Terraform module which a lambda @edge security lambda on AWS.

## Usage

```hcl
module "lambda-security" {
  source = "genstackio/website/aws//modules/lambda-security"

  // ...
}
```
