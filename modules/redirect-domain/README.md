# AWS Redirect Domain Terraform module

Terraform module which create a redirect domain on AWS.

## Usage

```hcl
module "redirect-domain" {
  source = "genstackio/website/aws//modules/redirect-domain"

  // ...
}
```
