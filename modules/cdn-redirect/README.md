# AWS CDN Redirect Terraform module

Terraform module which create a cdn redirect on AWS.

## Usage

```hcl
module "cdn-redirect" {
  source = "genstackio/website/aws//modules/cdn-redirect"

  // ...
}
```
